mkdir quiz2
cd quiz2
mkdir group_vars
cd group_vars
vi managed1-host-maulanamalik.yml
vi managed2-host-maulanamalik.yml
cd ..
vi ansible.cfg
vi inventory
vi secret.yml
ansible-vault encrypt secret.yml
belajaransible
vi quiz2-taks.yml
ansible-playbook --syntax-check --ask-vault-pass quiz2-taks.yml
echo 'belajaransible' > vault-pass
chmod 600 vault-pass
ansible-playbook --vault-password-file=vault-pass quiz2-taks.yml
RAND_NUMBER=$(python3 -c "import random; print(random.randrange(1,50))")
ssh pod-username-managed1 -l dev${RAND_NUMBER} 
ssh pod-username-managed1 -l ops${RAND_NUMBER} 
RAND_NUMBER=$(python3 -c "import random; print(random.randrange(51,100))")
ssh pod-username-managed2 -l dev${RAND_NUMBER} 
ssh pod-username-managed2 -l ops${RAND_NUMBER} 
